# Train all 5 algorithms in parallel
./train.sh

# Train only specific algorithms
./train.sh qmix mappo

# Live monitoring dashboard (refreshes every 5s)
./train.sh --status

# Kill all running training
./train.sh --kill